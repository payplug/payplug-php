<phpunit bootstrap="config.php">
    <filter>
        <blacklist>
            <directory suffix=".php">..</directory>
            <exclude>
                <directory suffix=".php">../lib</directory>
            </exclude>
        </blacklist>
    </filter>
</phpunit>